apiVersion: apps/v1
kind: Deployment
metadata:
  name: intellmeet-redis
  labels:
    app: intellmeet-redis
spec:
  replicas: 1
  selector:
    matchLabels:
      app: intellmeet-redis
  template:
    metadata:
      labels:
        app: intellmeet-redis
    spec:
      containers:
        - name: redis
          image: redis:7-alpine
          ports:
            - containerPort: 6379
---
apiVersion: v1
kind: Service
metadata:
  name: redis
spec:
  selector:
    app: intellmeet-redis
  ports:
    - protocol: TCP
      port: 6379
      targetPort: 6379
